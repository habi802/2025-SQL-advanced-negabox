<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.negaboxdummy.user.UserMapper">

    <insert id="userJoin"
            parameterType="kr.co.negaboxdummy.user.model.UserJoinReq"
            useGeneratedKeys="true"
            keyProperty="userId">

        INSERT INTO user
        (
        membership_id,
        name,
        email,
        password,
        birth,
        carrier_code,
        phone,
        point,
        created_at,
        updated_at,
        card_num,
        grade_updated_at
        )
        VALUES
        (
        #{membershipId},
        #{name},
        #{email},
        #{password},
        #{birth},
        #{carrierCode},
        #{phone},
        #{point},
        #{createdAt},
        #{updatedAt},
        #{cardNum},
        #{gradeUpdatedAt}
        )
    </insert>
</mapper>
